

  <% if viewable( seite, @current_user ) == true %>
    
    <% @role = @current_user ? @current_user.role : nil %>

    <%= render partial: "edit", locals: { seite: seite } %>

    <%= seite.display(@role).html_safe %>   
    
  <% elsif viewable( seite, @current_user ) == false %>
  
    <h1> protected content... </h1>
    
  <% end %>
  
 
  

  

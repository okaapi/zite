

	  
    <div id="files">
      <p>
      <table class="filetable">
        <% if @files and @files.count > 0 %>
          <tr><td></td><td></td><td>copy this address to insert above </td></tr>
        <% end %>
        <% @files.each do |f| %>
          <tr>

            <td> <%= link_to if_image(pagename, f), file_target( pagename, f) %></td>
            <td> <%= link_to truncate( file_basename(f), length: 30), file_target( pagename, f) %> </td>

            <td class="grab_box"> <%= file_grab_target( pagename, f) %> </td>
            <td> <%= link_to 'delete', 
                      file_delete_path( seite: pagename, filename: file_basename(f) ),
                      method: :post,
                      data: { confirm: 'Are you sure?' } %> &nbsp; </td>            
            <td> <%= file_encoding( f ) %> </td>
          </tr>
        <% end %>
      </table>
      </p>
    

      <%= form_tag file_upload_path( seite: pagename ), multipart: true do %>
        <p> 
          <%= file_field_tag :file, class: 'btn  btn-default' %>
          <p></p>
          <%= submit_tag 'Upload!', class: 'btn btn-default',
              data: { confirm: 'Are you sure? Your current edits will be lost...' } %> 
        </p>
        <p id="note"> Note 1: you will lose your edits... save first! <br>
         Note 2: all uploaded files are visible to anyone regardless of page permissions! </p>
      <% end %>

    </div>    


